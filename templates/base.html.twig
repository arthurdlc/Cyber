<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>{% block title %}Accueil{% endblock %}</title>
    <link rel="icon" type="image/png" href="../public/img/LOGO-300x300.png">
    
    <link href="/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link href="/css/style.css" rel="stylesheet">
    <link href="/css/all.css" rel="stylesheet">
    <link href="/css/fontawesome.css" rel="stylesheet">
    <link href="/css/brands.css" rel="stylesheet">
    <link href="/css/solid.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous"></link>
    <!-- Styles pour la navbar hamburger -->
    <style>
        body {
            margin: 0;
            padding: 0;
            overflow: hidden;
            background-color: rgb(14, 14, 14);
            color:  rgb(255, 0, 0);
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }

        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 0;
            height: 100%;
            background-color: black;
            transition: width 0.3s ease;
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        .navbar.open {
            width: 66.66%;
        }

        .menu-button {
            position: fixed;
            top: 20px;
            left: 20px;
            cursor: pointer;
            color: red;
            font-size: 24px;
        }

        .menu {
            list-style-type: none;
            padding: 0;
            margin: 0;
            position: absolute;
            top: 0;
            left: 0;
            transform: translate(-100%, 0);
            transition: transform 1s ease;
            text-align: center;
            top : 40% ;
            left : 10%;
            font-familly: fantasy;
        }

        .navbar.open .menu {
            transform: translate(0, 0);
        }

        .menu li {
            margin-bottom: 20px;
            text-align: center;
            opacity: 0;
            transform: translateX(-20px);
            transition: opacity 0.5s ease, transform 0.5s ease;
            font-size: 24px;
        }

        .navbar.open .menu li {
            opacity: 1;
            transform: translateX(0);
            transition-delay: 0.5s;
        }

        .menu a {
            text-decoration: none;
            color: red;
            display: block;
        }

        .close-icon {
            position: absolute;
            top: 10px;
            right: 10px;
            font-size: 24px;
            color: white;
            cursor: pointer;
        }
        
    </style>
</head>

<body>
    <!-- Bouton de la navbar hamburger -->
    <div class="menu-button" onclick="toggleNavbar()">☰</div>

    <!-- Navbar hamburger -->
    <div class="navbar">
        <!-- Icône de croix pour fermer la navbar -->
        <div class="close-icon" onclick="toggleNavbar()">✖</div>
        
        <ul class="menu">
            <li><a href="/">Accueil</a></li>
            <li><a href="/formation/show">Info-Formation</a></li>
            <li><a href="/login">Connexion</a></li>
            {% if is_granted('ROLE_ADMIN') %}
            <li><a href="/page">Modification de Page</a></li>
            <li><a href="/formation">Modification de formations</a></li>
            {% endif %}
            {% if app.user %}
            <li><a href="/logout">Déconnexion</a></li>
            {% endif %}
        </ul>
    </div>

    <!-- Votre contenu -->
    <div class="col-lg-12 mx-auto p-4 py-md-5">
        <main>
            {% block body %}{% endblock %}
        </main>
    </div>

    <!-- Scripts JavaScript -->
    <script>
        function toggleNavbar() {
            const navbar = document.querySelector('.navbar');
            navbar.classList.toggle('open');
        }
    </script>
</body>

</html>
