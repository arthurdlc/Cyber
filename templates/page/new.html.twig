{% extends 'base.html.twig' %}

{% block title %}FC-Pro | Nouvelle page{% endblock %}

{% block body %}
    <h1>{{'Create new Page'|trans}}</h1>

    {{ include('page/_form.html.twig') }}
    <br>
    <a class="btn btn-secondary" href="{{ path('app_page_index') }}"><i class="fa-regular fa-circle-left"></i> {{'back to list'|trans}}</a>
{% endblock %}
