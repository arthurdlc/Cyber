{% extends 'base.html.twig' %}

{% block title %}Page{% endblock %}

{% block body %}
    <h1>{{ page.title }}</h1>

    {{ page.text|raw }}


    <a href="{{ path('app_page_index') }}">{{'back to list'|trans}}</a>

    <a href="{{ path('app_page_edit', {'id': page.id}) }}">{{'edit'|trans}}</a>

    {{ include('page/_delete_form.html.twig') }}
{% endblock %}
