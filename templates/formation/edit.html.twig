{% extends 'base.html.twig' %}

{% block title %}Edit Formation{% endblock %}

{% block body %}
    <h1>{{'Edit Formation' | trans}}</h1>

    {{ include('formation/_form.html.twig', {'button_label': 'Update'| trans}) }}
    
    <div align="left">
    {{ include('formation/_delete_form.html.twig') }}
    </div>
    <div align="right">
        <a class="btn btn-secondary" href="{{ path('app_formation_index') }}"><i class="fa-regular fa-circle-left"></i> {{'back to list' | trans}}  </a>
        </div>
{% endblock %}
